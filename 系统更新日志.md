# 更新日志 · 个人AI办公能力测评（APCA）

> **版本口径说明**
> - `questionSetVersion`（web/data.js 中的题库版本）：随题库内容变更递增（v0.7 → v0.7.1 → v0.8 → v0.8.1 → …）；
> - 产品功能版本（v0.1–v0.7）：小程序/网站架构迭代；
> - 每个条目包含：变更内容、验证结果、部署状态。
>
> **发版惯例（自 v0.8.1 起固化）**
> 1. 任何影响题库内容、计分逻辑或用户可见行为的发版，**必须同步更新本文件**，新增条目置顶；
> 2. 版本号变更时同步修改 `tools/build-web-data.js` 中的 `questionSetVersion`；
> 3. `tools/verify-web.js` 已设强制门禁（漏更说明文档无法通过回归）：
>    - **§1.7**：当前 `questionSetVersion` 若无对应 CHANGELOG 条目 → 校验直接失败；
>    - **§1.8**：`tools/build-web-data.js` 与 `web/data.js` 版本号不一致（半成品发版）→ 失败；
>    - **§1.9**：`README.md` 版本速览表未含当前版本号 → 失败；
>    - **§1.10**：`系统更新日志.html` 缺失 / 不含当前版本 / 比 md 旧 → 失败；
>    - **§1.11**（v0.8.4 起）：固定单字母期望分超出「不可套利」区间 → 失败（防对外宣称再次失实）；
>    - **§1.12**（v0.8.4 起）：学习地图 tips 总数 ≠ 30 或仍有 `idx % 5` 错位 → 失败；
>    - **§1.13**（v0.8.4 起）：`产物/02-题库.json`、`产物/04-行业特化题包.json` 与真源不一致 → 失败；
   - **§1.6 + §1.6b**（v0.8.6 起）：§1.6 改为直接调用独立脚本 `tools/check-analysis-consistency.js`（单一真值源）；§1.6b 要求解析里「本题「最可能」是 X」的 X 必须等于该题满分锚点 → 失败。
> 4. **文档同步范围**（自 v0.8.2 起固化，用户明确要求）：每次系统更新，除本文件外还需同步——`README.md`（版本速览表 + 功能描述）、`产物/00-总览分析.md`（版本链路与产物清单）、`产物/05-网页版落地方案.md`（版本表与路线图）。涉及形态/口径变化时同步其余 `产物/*.md`。**自 v0.8.4 起，题库内容变更时还必须重跑 `tools/build-archive-snapshots.js` 重导 `产物/02`、`产物/04` 快照**（受 §1.13 门禁约束）。
> 5. 部署顺序：本地改码 → 升版本号（**只改 `build-web-data.js` 一处**）→ `node tools/build-web-data.js` 重建 `web/data.js` → `node tools/build-archive-snapshots.js` 重导产物快照 → 跑 `tools/build-qrcode.py` + `tools/build-update-html.js` 产出 `web/qrcode.png` 与 `系统更新日志.html` → 更新本文件与上述文档 → 跑五件套校验 → `rsync` 部署 `web/` + 上传 `系统更新日志.md` / `系统更新日志.html` → 重启 `apca.service` → 线上验证版本号。
> 6. **同步至 GitHub 仓库**（自 v0.8.5 起固化）：完成线上验证后执行 `./tools/sync-to-github.sh`，自动跑五件套校验、写带版本号的 commit message、`git push origin main`。详细操作与故障排查见 `GITHUB.md`（含 SSH key 配置、commit 规范、安全自查清单）。

---

## [v0.8.11] - 2026-09-18 · 移除内置默认口令 + 开源许可证落定

### 触发

为「将来是否转公开仓库」做前置排查时（清单见根目录 `内部过程文件/开源前清理清单_个人测评`），逐文件扫描出两处**现在就该修**的缺陷：

1. **口令硬编码**：`server/server.js` 写的是 `process.env.APCA_ADMIN_PASS || '<旧默认口令>'`。意思是没设环境变量时后台密码就是那个写死的值。仓库一旦转公开，等于把后台口令贴给所有人看；而"照文档跑起来、却忘了设环境变量"恰恰是最常见的漏改场景。
2. **版权与许可证三处不自洽**：根 `README.md` 的版权声明写的是"仅授权某一外部域名下免费使用、不得用于其他域名、不得商用"——这既与开源口径直接冲突，授权对象也不属于本项目自己；`README.md` 引用的 `./LICENSE` **文件根本不存在**（死链）；实际存在的 `web/LICENSE` 与 `LICENSE.template` 是"免费使用但禁止商用、禁止部署到其他域名"的个人作品许可，不是开源许可证。

### 主要变更

- **服务端不再内置任何口令** — `server/server.js`：未设置 `APCA_ADMIN_PASS` 时**拒绝启动**（退出码 1），并打印四行处置指引（给出完整命令、说明只想做问卷可直接打开 `web/index.html` 走本机模式）。删掉 `USING_DEFAULT_PASS` 变量。注意：只想做测评的人不经本服务，这条变更不影响问卷作答。
- **启动日志不再打印明文密码** — 原逻辑在"使用默认密码"时会把密码原文打进控制台；现在一律输出 `******`，并移除那条默认密码告警分支（已经不可能触发）。
- **许可证改为 MIT** — 新增仓库根目录 `LICENSE`（MIT 正本，Copyright © 2026 jmount）；`LICENSE.template` 由原"限域名、禁商用"的个人作品许可改写为 **MIT 副本 + 部署登记信息**（部署位置/对外地址/版本三项占位保留，`tools/build-license.py` 的双服务器渲染机制不变）；根 `README.md` 版权声明改写为"源代码按 MIT 许可证开源"。
- **文档同步** — `server/README.md`：启动命令补 `APCA_ADMIN_PASS`、密码表改为"无默认值（必填）"、部署建议里的"改掉默认密码"改为"设置一个强密码"；根 `README.md` 的服务器差异对照表相应调整。

### 部署须知（重要）

本次改了服务端启动行为。**两台在用服务器的服务配置里都已经带着 `APCA_ADMIN_PASS`**（公网机在 systemd 的服务配置里，内网机在 `/etc/apca/apca.env` 里，由部署脚本随机生成），所以升级后重启**不会**起不来。但**任何新装的实例都必须先设这个变量**，否则服务会拒绝启动——这是有意设计的。

### 验证

- 未设密码启动：进程退出码 1，输出四行指引 ✅
- 设置密码后正常启动、后台登录鉴权正常 ✅（服务端端到端已覆盖）
- 五件套回归：**508 PASS / 0 FAIL**

### 部署状态

本地改码 + 文档同步完成，**尚未 rsync 上线**（待安排）。

### 仓库治理补充（2026-09-18 · 不影响题库与算法，版本号不变）

同日按「转公开前置」做了一轮仓库脱敏。**线上跑的题库与算法完全没动，也不需要重新上线**：

- **部署信息换成中性写法** — 服务器 IP、对外域名、SSH 登录账号与密钥文件名、部署目录、环境配置文件路径，一律改为 `<服务器IP>` / `<你的域名>` 这类占位。
- **构建脚本不再内置地址** — `build-license.py`、`build-qrcode.py`、`build-framework-bg-card.py` 的地址改为部署时用命令行参数或环境变量传入；不传会提示"这是占位值，勿用于部署"。
- **内网部署手册移出仓库** — 针对另一主体的内网部署章节（README 原「五 B」整节）与专用部署脚本移到工作区 `内部过程文件/`，README 只留一句指引。
- **上线脚本改名并参数化** — `tools/deploy-lzhu.sh` → `tools/deploy.sh`；目标主机、部署目录、对外地址全部由环境变量传入，不设主机别名会直接报错退出（不会误连到别的机器）。
- **部署期物料不再入库** — `web/qrcode.png`（含本机入口地址）与 `web/LICENSE`（含部署登记信息）加入 `.gitignore`，部署时才生成。
- **仓库历史清零重建** — 只改当前文件的话，旧提交里的地址仍能被翻出来，所以用脱敏后的文件重新初始化仓库并强推。

> **下次上线须知**：`web/data.js` 的 `authorizedDomain` 现在是空的（重建题库后未注入）。
> 同步 `web/` 之前先跑一次
> `python tools/build-license.py --domain=你的域名 --name=显示名`，否则线上"关于"页会显示占位串。


---

## [v0.8.10] - 2026-09-13 · 专项卷复盘降级文案（明确引导，避免「画像信息缺失」不明不白）

### 触发
v0.8.9 上线后复盘兜底逻辑（`reviewHTML` 在画像推不出时自动展开全部 + 提示「画像信息缺失」）对**专项卷作答用户**不友好：专项 18 题（招投标/制造业/零售业）目前未挂 abilities 标签（v0.9.0 题库能力点 V2 专项处理），所以用户作答后画像能力点为空，复盘默认折叠逻辑判定为「无相关题」→ 全展开 + 那句不明不白的「画像信息缺失」对用户没有明确下一步。本版本仅修复**降级文案**（不影响题库与算法），让专项用户看到明确引导。

### 主要变更
- **`web/app.js` `reviewHTML()` 兜底分支分流**：
  - `record.packId !== 'none'`（专项卷作答）→ tip 文案：「本次为专项卷作答（X 题），能力点级解读需配合通用 32 题作答开启；本次仅按维度+动作给出建议，建议先做一次通用 32 题获得能力点级画像。」（不再写「画像信息缺失」）
  - `record.packId === 'none'`（通用卷或分享链接异常）→ 保留「画像信息缺失，已展开全部题目（分享链接或作答数据异常）」兜底说明
- tip 通过 `lastIndexOf('</div>') + slice` 插到 review-list 闭合之前（renderResult 里的「展开全部题目 ▼」按钮在 block-h 仍正常显示 + 可切换）
- **`tools/verify-web-dom.js`** J 段（专项 standalone 流程）新增 2 条断言：
  - `review-list data-showall === '1'`（兜底真的触发了）
  - tip 同时包含「本次为专项卷作答」+「能力点级解读需配合通用 N 题作答开启」

### 题库与算法
- **未改题库内容**——v0.8.10 是文案降级，qVersion **不 bump**（仍为 v0.8.10 同元数据，但题库/算法与 v0.8.9 完全一致；v0.8.10 仅 framework 文案字段递增）
- abilities 标签覆盖现状：核心 32 题 100%（平均 1.3 个/题） / 专项 54 题 0%——后者将由 v0.9.0 题库能力点 V2 专项处理

### 验证
```
五件套: 508 PASS / 0 FAIL  (verify-web 360 + verify-web-dom 51 + verify-wiring 19 + verify-merge 11 + verify-server 67)
独立一致性: 86 题 硬矛盾 0 / 软警示 0
```

### 部署状态
- ✅ 老服务器 <服务器IP>：`rsync web/ + server/` → `systemctl restart apca.service` → 线上验证 `framework=APCA v0.8.10`
- ✅ 合作方内网服务器（内网 <内网服务器IP>，对外 `https://<内网域名>/ai-assess/`）：2026-09-14 经内网 SSH 更新——备份 `<部署目录>_backup_20260914_1151`（含 `records.json`）→ 解压覆盖 `web/` + `server/`（**保留 `server/data/`**）→ `chown -R root:root` → `systemctl restart apca` → 验证通过：
  - 本机 `framework=APCA v0.8.10` / `authorizedDomain=<内网域名>` / `api/ping` 通 / 记录数仍为 1（未串数据）
  - 公网 `/ai-assess/` 返回 200，无尾斜杠 `/ai-assess` 正确 301 补斜杠 → 确认 nginx 前缀剥离配置本来就正确，**未改动 nginx**
  - 公网 `qrcode.png` md5 = 域名版（`c0405fd4...`），入口二维码指向子路径无误
  - 真实浏览器端到端：hero 显示 `APCA v0.8.10`、信息页 → 答题页（第 1/32 题，选项已乱序）正常渲染

### 待办
- v0.9.0 题库能力点 V2：三位行业评审对专项 54 题能力点分类 + D.dimensions[].abilities 跟 q.abilities 映射重过 + verify-web §1.6b 升级 + qVersion 升至 v0.9.0 并明确「能力点级解读仅 v0.9.0+ 起记」
- GitHub 同步（同 v0.8.9，待 SSH 公钥补到 https://github.com/settings/keys）

---

## [v0.8.9] - 2026-09-13 · 全量整改 Codex 回归审查（服务端计分口径对齐 / 完整度收紧 / 安全加固 / 双服务器二维码 / D3 中性化）

### 触发
2026-09-13 用户提供**第四个独立工作台 Codex** 针对**线上 v0.8.8** 的回归审查报告（PDF，14 页）。该报告抓到了前三份都没发现的**严重回归**：v0.8.7 新增的服务端重算与前端专项加权模型不一致，导致专项卷/单独成卷记录大面积被服务端拒绝入库。本版本按「全修」口径完成 P0 + P1 + P2 共 13 项整改。

### 主要变更
**P0 计分正确性（生产事故级）：**
- **P0-1 服务端加权口径与前端对齐**（回归修复）：`server/server.js` `serverCalcResult()` 弃用「每维度题目数」加权（每专项每维恰好 3 题 → 等价六维等权），改用 `pack.weights` 声明权重，与前端 `web/app.js:439/452 → 94-99` 同源；新增 `merged` 参数，合并出分路径走等权（对应前端 `app.js:542` 显式传 null）。**此前专项 standalone 被拒 77~81%、blended 被拒 57~69%，最大偏差 6.6 分 → 修复后偏差 ≤0.5、全部入库。**
- **P0-2 完整度门槛收紧**：`web/app.js:115` + `server/server.js:134` 判定由 `completeness < 0.5` 改为 `< 1`——**只有全部题作答完成才生成等级**。此前 18/32（0.5625）仍出 L4 并被服务端入库，属刷分缺口。
- **P0-3 门禁补维度不均衡回归用例**：`tools/verify-server.js` 新增 2 条永久用例——①bid standalone（D4=0 其余 100）期望服务端重算 total=78.0；②merged 路径（核心全 0 + 专项全 100）期望等权 ≈36.3。**此前用例全是「专项全 100 分」，两套加权模型结果都是 100，天然覆盖不到该回归。**

**P1 安全与合规：**
- **P1-1 公网更新日志脱敏**：`tools/build-update-html.js` 新增 `sanitize()`——生成公网 HTML 时屏蔽服务器 IP、SSH 目标、部署/同步脚本名与 systemd 服务名；仓库内 `系统更新日志.md` 保留技术细节（开发者文档）。
- **P1-2 CSV 公式注入**：`web/admin.js` 导出 CSV 的 `cell()` 对 `=`/`+`/`-`/`@`/Tab/CR 开头的字段加单引号前缀，防 Excel/LibreOffice 把用户可控字段（姓名/公司/岗位/批次/备注）当公式执行。
- **P1-3 静态资源安全头**：`server/server.js` `serveStatic()` 对 `.html/.htm/.js/.css` 补 `X-Frame-Options: DENY` + `Content-Security-Policy`；所有静态资源补 `X-Content-Type-Options: nosniff` + `Referrer-Policy: no-referrer`。此前 v0.8.8 S-04 只在 `sendJSON()` 加，CSP/X-Frame-Options 作用不到文档响应。
- **P1-4 分享链接防伪造**：`web/app.js` 分享 payload 不再携带 `total/levelId/dimScores`；`tryLoadShared()` 一律按 `payload.answers` 重新调用 `calcResult` 计算分数与等级。任何人手工构造的「100 分 / L4」分享链接都会被重算覆盖。
- **P1-5 二维码双服务器版本**：`tools/build-qrcode.py` 重写为双版本生成——主站版指向 `http://<服务器IP>:8787/`（**用 IP**，因 `<你的域名>` 根路径是公司官网而非测评系统），内网版指向 `https://<内网域名>/ai-assess/`（用域名 + 子路径：该服务器上测评挂在 `/ai-assess/` 子目录，不是域名根路径）。输出分别为 `web/qrcode.png` 与 `产物/qrcode_site-b.png`，各自部署到对应服务器。

**P2 内容与体验：**
- **P2-1 海报版本号动态化**：`tools/render-poster-preview.py` 两处硬编码 `v0.8.3` 改为读 `VER`（来自 `build-web-data.js` 单点真值），此前发版后海报版本号不跟随。
- **P2-2 D3-6/D3-7 外部引用中性化**：`miniprogram/data/core.js` 两题解析中的机构名与产品名（Chroma / Laban / Morph / LawVu / 豆包工作 / QwenWork / WorkBuddy / ChatGPT）改为「业界 N 年公开评测 / 实践反馈」等中性表述，保留量化结论（10-25%、39%、50K 等）。
- **P2-3 复盘关联题去 D1 偏置**：`web/app.js` 新增 `pickWeakAbilitiesByDim()`，复盘默认折叠的「关联能力点」由 `globalWeakAbilities`（全卷最弱 3 个，受 D1 题多+能力点多影响）改为「维度分升序取 3 个维度、每维取该维最弱能力点」，与 `buildAdviceForRecord` 同款逻辑。独立验证脚本 `tools/check-review-dim-bias.js`：D1 占比 36.5% → **17.7%**（接近理论均匀值 16.7%）。
- **P2-4 画像口径明示**：54 道专项题不含 `abilities` 标签、不参与个体化画像（设计如此）。`web/app.js` 在专项卷/合并卷的建议区增加「画像口径: 基于通用卷作答」徽标（鼠标悬停说明），避免用户误以为专项作答也进入画像。**未给专项题补标签**——补标签会改动画像算法与 54 道题内容，属设计变更，需单独评估后另行发版。
- **P2-5 后台完整度展示**：`web/admin.js` 记录详情新增「作答完整度」行（百分比 + 实际作答题数 + 未答完提示），配套 P0-2 的缺答治理。

**测试增强（不影响产品行为）：**
- 承接 v0.8.8 遗留的 D-01/D-02：新增 `tools/lib/b64safe.js`（0x8000 分块 base64，`verify-web-dom.js` 改调 helper）；`web/admin.js` 新增 `S.serverUnreachable` 标志，公网后端不可达时给出运维级错误文案（区分「本地登录已禁用」与「后端服务不可达」）。

### 验证结果
```
五件套: 506 PASS / 0 FAIL
  verify-web 360 + verify-web-dom 49 + verify-wiring 19 + verify-merge 11 + verify-server 67
独立一致性: 86 题 硬矛盾 0 / 软警示 0
计分口径对齐: tools/check-backend-front-scoring-parity.js — 三专项 blended/standalone/merged 偏差全部 ≤0.5
复盘维度偏置: tools/check-review-dim-bias.js — D1 占比 35.5% → 17.6%
```
- `tools/check-backend-front-scoring-parity.js`：独立复现两套加权模型（不 require app.js / server.js）。极端场景「bid standalone, D4=0 其余 100」前端 78.0 / 旧服务端 83.3（偏差 5.3）→ 修复后两端**完全一致 0.0**；三专项 × blended/standalone × 3000 组随机不均衡作答，**不一致 0 组、最大偏差 0.0**（旧模型会被拒 279~411/500）。merged 路径两端一致 36.3。
- `tools/check-review-dim-bias.js`：500 组随机作答，复盘 D1 偏置 36.5% → **17.7%**（接近理论均匀值 16.7%）。

### 部署状态
- ✅ 老服务器 <服务器IP>：`rsync web/ + server/` → `systemctl restart apca.service` → 线上验证通过（版本号 APCA v0.8.9 / 4 安全头覆盖静态资源 / 分享链接重算 / 二维码指向 IP 入口）
- ⬜ 合作方内网服务器 <另一台服务器IP>：由用户在内网手工同步；同步前先跑 `python tools/build-qrcode.py --server=site-b` 与 `python tools/build-license.py --server=site-b`，再 rsync（域名授权 <内网域名>，二维码指向子路径入口 `https://<内网域名>/ai-assess/`）

---

## [v0.8.8] - 2026-09-12 · 安全/文案/计分复盘整改（admin 密码/30题口径/ping 信息/安全头/b64 栈溢出）

### 触发
2026-09-12 用户提供**第三个独立工作台 TRAE SOLO**针对 v0.8.6 的二次审查报告（HTML 格式，9 项 finding），与之前 v0.8.6 PDF 报告交叉对照，发现：唯一被两份独立审查同时点出的真 bug 是「通用 30 题」硬编码；其他 6 项 HTML 独家 finding（admin 密码 / ping 信息 / 安全头 / b64 栈溢出 / 0-33-67-100 / 全站 HTTP）均为真实存在但当时漏检的安全/文案隐患。本版本一并整改。

### 主要变更
**P0 安全（必修）：**
- **S-01 admin 密码硬编码**: web/admin.js 删除 `LOCAL_USER = 'admin' / LOCAL_PASS = '<旧默认口令>'` 两个明文常量；本地模式 fallback 加 `isLocalHost()` 守卫，仅 `localhost / 127.0.0.1 / file://` 允许无口令快速预览，任何公网 host 一律拒绝本地登录、强制走服务端鉴权（server.js /api/admin/login）。

**P1 文案与计分精确化：**
- **B-01 「通用 30 题」硬编码**: web/admin.js:415（合并出分提示）、web/index.html:43（首屏默认「通用 30 题 + 专项 N 题」）、web/index.html:115（首屏默认「第 1 / 30 题」）全部改成动态——admin.js 用 `D.core.length`，index.html 用「通用 ? 题 + 专项 ? 题（启动后自动填充）」+「第 ? / ? 题」占位避免首屏闪现旧值；app.js 头注释同步更新。
- **B-02 「0 / 33 / 67 / 100」文案**: web/app.js 复盘说明 + web/index.html 关于页计分说明，改为与 scoreMap 一致的 `0 / 33.3 / 66.7 / 100`。

**P1 安全响应：**
- **S-03 /api/ping 信息泄露**: server.js:518 删除 `count: records.length` 字段，仅返回 `{ok:true, service:'apca-admin-by-jmount'}`（/api/admin/ping 鉴权后的 count 字段保留，供后台管理员使用）。
- **S-04 缺安全响应头**: server.js `sendJSON()` 加 4 个响应头：`X-Content-Type-Options: nosniff`、`X-Frame-Options: DENY`、`Referrer-Policy: no-referrer`、`Content-Security-Policy: default-src 'self'; img-src 'self' data:; script-src 'self'; style-src 'self' 'unsafe-inline'`。

**P2 代码质量：**
- **C-01 b64encodeUnicode apply 栈溢出隐患**: web/app.js 改为 0x8000 字节分块拼接（块大小 32768 远低于 V8 ~65535 上限），当前 32-50 题 payload 实际触发概率很低但修掉避免日后超长分享链接踩雷。

### 验证
- 五件套 **500+ PASS / 0 FAIL**（完整跑 verify-web / verify-web-dom / verify-wiring / verify-merge / verify-server）
- 独立一致性脚本（解析-分值一致性）：硬矛盾 0 / 软警示 0（86 题）
- /api/ping 响应不再含 `count` 字段（curl 验证）
- admin.html 公网部署下本地登录 fallback 拒绝（curl POST 模拟）

### 部署
- 老服务器 <服务器IP>: `rsync web/ + server/` → `systemctl restart apca.service` → curl 验证 framework=APCA v0.8.8
- 合作方内网服务器 <另一台服务器IP>: 由用户在**内网手工**跑 `python tools/build-license.py --server=site-b` 后再 rsync 同步（避免公网重复授权主站）

---

## [v0.8.7] - 2026-09-12 · 交叉审查整改（缺答/刷分/解读偏 D1/30 天路线/服务端重算/LICENSE 双服务器）

### 触发
2026-09-11 用户部署 v0.8.6 给**另一个工作台**做独立交叉审查，回收 13 页 PDF 报告。报告梳理出 3 严重 + 5 较高风险 + 6 一般问题，10 条完全属实。本版本就其中 P0 部分整改。

### 主要变更
**P0 安全与正确性（必修）：**
- **P0-2.1 缺答即可 L4 / 伪造入库**：calcResult 返回 completeness（作答题数/总题数），completeness < 0.5 时 level=null；服务端 /api/submit 独立重算 total+dimScores（不信任客户端提交值），偏差 > 0.5 拒绝；入库存重算结果。
- **P0-2.2 个体化解读 72% 回退模板**：buildAdviceForRecord 改按 dimScores 升序选最低维度 → 在该维度内 abilityScores 升序选最弱能力点，不再用偏到 D1 的 globalWeakAbilities（74.7%）。
- **P0-2.3 30 天路线输出 21-36 天**：buildLearningMap 改为固定 6 维 × 5 天 = 30 天（弱 2 维走画像动作，其余维度走模板，弱项补足至 5 条/维）。
- **P0-3.3 LICENSE 双服务器差异部署**：删除旧 LICENSE，新增 LICENSE.template + tools/build-license.py 按 `--server=site-a|site-b` 渲染。主站服务器授权 <你的域名>，合作方内网服务器授权 <内网域名>。data.js meta 新增 authorizedDomain 字段。
- **P1-3.1 跨版本合并未按 qVersion**：findGeneralRecord 加 qVersion 校验，跨版本返回 null + UI 提示「你之前的通用测评是旧版本, 单独成卷前请先重答当前通用卷」。
- **P1-3.2 同步失败前端 toast**：syncToServer 改为成功 toast / 失败弹「同步失败」+ 持久化 record 到 localStorage，下次提交自动重试。
- **P1 复盘兜底空操作**：reviewHTML 中 items.forEach(() => no-op) 改为真实现：画像缺失时自动展开所有题 + 切换按钮替换为说明文案。

**P2 文案修正：**
- 「通用 30 题 + 本专项」→「通用 ' + D.core.length + ' 题 + 本专项」动态（v0.8.6 已 32 题）
- 「选项顺序随机打乱」→「每题选项按固定规则乱序呈现」（更精确表述）
- 二维码默认 URL 由 `http://<服务器IP>:8787/` 改为 `https://<你的域名>/`，可通过 APCA_ASSESS_URL 覆盖

**工具与门禁：**
- tools/build-web-data.js: framework + questionSetVersion 同时升 v0.8.7
- miniprogram/utils/calc.js: 小程序端 calcResult 也加 completeness 校验（与 web 端同步）
- verify-web.js: 新增 §5 完整性 + 低完整度不出综合等级回归；新增 §6 buildLearningMap 30 天固定回归；新增 §7 buildAdviceForRecord 不再用 globalWeakAbilities 回归
- tools/verify-server.js: RECORDS mock 用 32 题全 D（与服务端重算口径一致）+ bids 题 id 修正为 BID-1..18
- tools/build-qrcode.py: 默认 URL 改域名
- 新增 LICENSE.template + tools/build-license.py（双服务器差异）

### 验证
- 五件套 **495 PASS / 0 FAIL**（verify-web 355 + verify-web-dom 47 + verify-wiring 19 + verify-merge 11 + verify-server 63）
- 独立一致性脚本（解析-分值一致性）：硬矛盾 0 / 软警示 0（86 题全覆盖）
- buildLearningMap 输出恒为 30 天（6×5），第 i 条标 Day i+1
- 服务端重算对客户端伪造 total 偏差超 0.5 自动拒绝；缺答 < 0.5 不出综合等级

### 部署
- rsync web/ → 部署用户@<服务器IP>:<部署目录>/web/（同步 LICENSE + authorizedDomain 已切到 <你的域名>）
- <另一台服务器IP> 合作方内网服务器：由用户去内网同步，同步时跑 `python tools/build-license.py --server=site-b` 重新生成正确的 LICENSE

---

## [v0.8.6] - 2026-09-11 · 基于作答画像的个体化解读 + D3 维度 +2 道题（Lost in the middle / 多轮失忆）

> 本轮为「解读是否真实有用」复盘的整改：
> 把「**同段位同建议**」这种**花架子风险**拿掉，让"最该做的 3 件事"基于本次作答画像生成；
> 同时把 D3 维度补到 7 题，覆盖 2026 年长文档失忆与多轮对话失忆这两个**普通 AI 协作场景下仍真实存在**的痛点。

### 变更

**A. 个体化解读（基于作答画像）**
- 在 `miniprogram/data/reports.js` 新增 `ACTION_DICT`（24 能力点 × 3 具体动作 = 72 条），按维度 id × 能力点 key 索引。
- 在 `web/app.js` 新增 `analyzeProfile(record)`：
  - 解码每题 `abilities` → 按维度 × 能力点累加 → 按维度实际题数归一化 → 0-100 分的能力点评估；
  - 推导 `byDim`（每维度的弱能力点 + 强能力点）、`globalWeakAbilities`（全卷最弱 3 个）、`globalStrongAbilities`（全卷最强 2 个）。
- 重写 `buildAdviceForRecord(record)`：基于画像取最弱维度的最弱能力点 → 取 `ACTION_DICT` 的 3 条具体动作（按降序）→ 拼成「本周重点练 / 推进路径 / 把强项沉淀为团队资产」3 条建议。
- 重写 `buildLearningMap(dimScores, abilityScores)`：30 天动作从「按维度模板铺」升级为「按画像从 ACTION_DICT 取具体动作」（缺画像时回退模板）。
- 重写 `reviewHTML(record, profile)`：默认**折叠**到本次解读提到的能力点相关题（约 5-10 道），顶部加「展开全部题目」按钮可切换。

**B. D3 维度新增 2 道题（v0.8.6 必做）**
- 通用卷 30 → 32 题（D3 5 → 7 题）。
- D3-6「Lost in the middle」：80 页合同场景，AI 一次装下但中间条款被弱化（D 是让 AI 重新聚焦中间）。
  - 题目依据：Chroma Research 2025（18 模型 lost in the middle）/ Morph 2026（200K 在 50K 退化）/ LawVu 2026（分块重审仍出现编造）等。
- D3-7「多轮对话上下文锁定」：跨 8 轮对话 AI 忘记早期约束（D 是把跨轮约束沉淀为外置清单）。
  - 题目依据：Laban et al. 2025（多轮平均性能下降 39%）/ Morph 2026 / Chroma Research 2025。
- 旧的两版 D3-6 题面（Agent 编排、长文档分块 → 弹窗仅处理 30 页）均被打回作废——前者太工程化非通用卷画像、后者 2024 痛点 2026 已不存在。

**C. 题面 abilities 标签字段（30 题全部补齐）**
- 在 `core.js` 每道题加 `abilities: [...]` 字段，按 `q.id` 维度前缀归到对应能力点（每题 1-2 个能力点）。

**D. 发版验收中发现并修复的缺陷（D3-6 / D3-7 选项与分值错位）**
- **现象**：受测人反馈最后一题「解析说某选项是最优解，但界面把 100 分标在了另一个选项上」，解析与分值自相矛盾。
- **根因**：系统硬规则为「源数据选项 A→D 即成熟度升序（A=0 / B=33.3 / C=66.7 / D=100）」，构建脚本按**位置**赋分。新增两题时按"自然叙述顺序"排了选项，未按成熟度升序排列 → 解析（按内容写）与分值（按位置赋）打架。
- **修复**：两题选项按成熟度升序重排，解析内的字母引用与结论一并重映射——
  - D3-6：A 接受 AI 意见(0) → B 分 3 段重审(33.3) → C 自己过一遍(66.7) → D 让 AI 重新聚焦中间(100)；
  - D3-7：A 换更长上下文模型(0) → B 拆成独立短对话(33.3) → C 再粘贴一遍约束(66.7) → D 沉淀为跨轮约束清单(100)。
- **门禁补强（关键）**：独立校验脚本 `tools/check-analysis-consistency.js` **已抓到**这 2 题硬矛盾，但 `verify-web.js` §1.6 用的是自维护的**残缺正则**（只认「最强 / 最高级 / 最稳的起点」，不认「最优 / 最稳做法」），**且从未调用该独立脚本**，导致漏网。本轮改为：
  1. **§1.6 直接调用独立脚本**（单一真值源，规则不再漂移）；
  2. **新增 §1.6b**：解析里凡写「本题「最可能」是 X」的，X 必须等于该题满分锚点（源数据不带 `scores`，锚点恒为 D）。

### 验证

- **五件套 491 PASS / 0 FAIL**：verify-web 351 + verify-web-dom 47 + verify-wiring 19 + verify-merge 11 + verify-server 63
- **独立一致性脚本 `check-analysis-consistency.js`：硬矛盾 0 题 / 软警示 0 题**（86 题全覆盖）
- §1.6 改造后改由独立脚本驱动（单一真值源）；新增 §1.6b「解析结论指向满分锚点」断言 1 条
- 题库内容变更：通用 30 → 32 题（D3 5 → 7 题）；满分项字母分布 {A:19, B:25, C:20, D:22}（86 题）
- §1.13 真源与导出可配对题数 86（v0.8.5 基线 84 → +2）；选项长度均衡（D3-6/D3-7 均在 1.67 阈值内）

### 部署

- ✅ **老服务器 / 原始服务器（本公司 <服务器IP>）**：`rsync --delete` 同步 `web/` → `<部署目录>/web/` + scp `系统更新日志.md`（→ `<部署目录>/`）与 `系统更新日志.html`（→ `<部署目录>/web/`）→ `systemctl restart apca.service` → 源站验证 `APCA v0.8.6` + 32 通用题（含 D3-6 / D3-7）通过。**修复版（选项重排）后再次同步并重启，源站验证通过。**
- ⬜ **合作方内网服务器（<另一台服务器IP> / <内网域名>/ai-assess/）**：需在内网更新（`tools/deploy-site-b.sh`），由管理员执行。

---

## [v0.8.4] - 2026-09-07 · 交叉审查整改（学习地图 30 天 / 宣称勘误 / 解析裁决 / 产物门禁）

> 本轮为三份第三方交叉审查报告（另见 `交叉审查裁定_v0.8.3_20260904.md`）的**批次 A + 批次 B** 整改。

### 变更

**A-1 学习地图修为真正的 30 天（用户可见缺陷）**
- 原 `buildLearningMap()` 每个维度只 `push` 1 天 → 全库只输出 **6 天**，与结果页标题「30 天提升路线」不符；
- 且 `TIPS[dim][idx % 5]` 用**维度排名**当 tips 下标（第 6 名维度错位取回 `tips[0]`），内容重复。
- 现改为：维度按得分升序（弱项优先），每维度连续铺满其全部 5 条 tips → **6×5 = 30 天**，下标不再错位。

**A-2 「无脑选 D ≈ 35 分（L1）」对外宣称勘误**
- 实测全库 84 题**固定单字母期望分**：A 47.2 / B 55.2 / C 48.4 / D 49.2（随机作答期望≈50）。原宣称的「约 35 分（L1）」**失实**（固定选 B 甚至可达 L3 低区）。
- `README.md` 版本速览表、`产物/05-网页版落地方案.md` 更正为「固定单字母最高约 55 分，不达 L4，无规律可套利」；本文件 v0.8 条目加勘误标注。
- `tools/verify-web.js` **新增 §1.11 回归断言**锁死该口径：最高 < 76（不可达 L4）/ 最低 ≥ 40 / 极差 ≤ 20 / 100 分选项覆盖四个字母 / 单字母承载 100 分题占比 ≤ 40%。

**A-3 清理 v0.7 版本残留**
- 关于页 `题库 v0.7` 硬编码 → 改为动态读取 `meta.questionSetVersion`；
- `index.html` hero 与关于页两处静态 `v0.7` → 改为 `APCA`（由 JS 填充当前版本）；
- `web/data.js` 头注释、`web/app.js` / `tools/build-web-data.js` 注释中的 v0.7 表述清理；
- 本文件 v0.8.3 条目的「⬜ 待部署」更正为已上线。

**A-4 15 题「解析与分值矛盾」清零**
- 源数据选项为成熟度升序（A=0 … D=100），但 15 道题的解析未把最高级定性落在 D 上；其中 **D1-2 解析推崇 C（66.7）却弱化 D（100）**，属解析与计分口径冲突。
- 逐题改写（`tools/patch-analysis-v084.py`，一次性补丁）：D1-1 / D1-2 / D1-3 / D1-4 / D1-5 / D2-2 / D2-3 / D2-4 / D2-5 / D3-2 / D3-3 / D3-4 / D6-2 / D6-3 / D6-4。
- `check-analysis-consistency.js`：软警示 **15 → 0**，硬矛盾保持 **0/84**。

**B-1 产物/ 快照重导 + 新增 §1.13 门禁（原门禁盲区）**
- `产物/02-题库.json` 与真源有 **19/30 题**选项不同；`产物/04-行业特化题包.json` 仍写 4 包 21 题（geo/gov），真源实为 **bid/mfg/retail 3 包 54 题**。
- 新增 `tools/build-archive-snapshots.js`：从 `miniprogram/data/` 真源重导两份快照，版本从 `build-web-data.js` 单点真值读取，并写入 `source.version`。
- `tools/verify-web.js` **新增 §1.13**：快照必须存在、版本标注等于当前版本、且与真源**逐题（选项 + 解析）比对一致**。

**B-2 权重口径统一**
- 关于页展示各维权重，但通用卷实际六维等权（`app.js` 固定 `/6`）。已在「6 大能力维度」区新增说明：**权重仅在专项卷参与加权，通用版六维等权**。

**工程卫生**
- `verify-web.js` 原 L42 硬编码 `v0.8.3` → 改为从 `build-web-data.js` 读取单点真值，往后发版无需再改校验脚本；
- 新增 **§1.12 学习地图门禁**（tips 总数 = 30、每维 5 条、`tips.length` 循环、禁用 `idx % 5`、标题一致）。

### 验证
- `node tools/check-analysis-consistency.js`：硬矛盾 **0/84** · 软警示 **0/84**（原 15）；
- `node tools/verify-web.js`：**337 PASS**（新增 §1.11 / §1.12 / §1.13 共 13 条断言全绿）；
- 题库与计分口径未变：乱序分布仍 A:19 / B:25 / C:18 / D:22，恒等排列 6 题；
- 待跑 verify-web-dom / verify-wiring / verify-merge / verify-server 五件套后才算全绿。

### 部署
- ✅ 已上线（2026-09-07）：`rsync --delete` 同步 `web/`（含 `data.js`/`app.js`/`index.html`/`qrcode.png`）→ `scp 系统更新日志.md` 到 `<部署目录>/`、`系统更新日志.html` 到 `<部署目录>/web/` → `systemctl restart apca.service`。线上验证：`data.js` 版本号 `v0.8.4`（3 处）、`app.js` 学习地图为按短板铺满 30 天、`index.html` 与 `app.js` 用户可见面已无 `v0.7` 残留（仅保留一条说明性代码注释）、首页/二维码/更新日志 HTML（URL 百分号编码）均 200。

---

## [v0.8.5] - 2026-09-07 · 计分归一化 + 乱序独立校验 + 服务端加固（批次 C + 批次 D）

> 本轮为三份第三方交叉审查报告的**批次 C（计分与数据） + 批次 D（工程卫生）**整改。
> 题库内容、解析措辞、乱序分布均未变化（与 v0.8.4 完全一致）；仅修正计分/校验/服务端的**实现缺陷**。

### 变更

**C-1 blended 分母按实际作答维度归一化（计分口径缺陷）**
- 原 `calcResult()` 在通用卷分支硬编码 `/ 6`。当有维度未作答时（如中途 confirm 提交），`dimScores` 键数 < 6，分母仍为 6 → 系统性**低估**总分；
- 且与 standalone 分支按「实际权重和 `sw` 归一化」的口径不一致。
- 改为：按**实际参与计分的维度数**归一化（六维等权逻辑不变）。涉及 4 处复刻：`web/app.js` + `tools/verify-web.js` + `tools/verify-merge.js` + `miniprogram/utils/calc.js` 全部同步。
- `verify-web.js` **新增 §1.15 回归断言**：构造「只答 3 维且全满分 → 总分 100」「六维全满分 → 100」「六维全 0 → 0」三个场景锁定。

**C-2 乱序结果独立交叉校验（用自己验自己的盲区）**
- 原 §1.5 校验与构建共用 `tools/lib/shuffle.js`，只能证明"构建跑了乱序"，抓不出乱序实现本身的 bug（如分值没跟选项走、某一档分值丢失/重复）。
- 新建 `tools/lib/shuffle-indep.js`：**完全不引用 `shuffle.js`**，靠"源题选项文本 ↔ 导出题选项文本"反查，独立推导出每题各展示字母应有的分值。前提（每题四选项文本互异）已实测全库 0 例重复。
- `verify-web.js` **新增 §1.14**：4 条断言
  - ① 真源/导出可配对题数 = 84；
  - ② 每题四选项文本互异（保证可反查）；
  - ③ 独立推导分值与导出 `q.scores` 全部一致（选项与分值未错位）；
  - ④ 满分项字母分布回归锁定 A:19 / B:25 / C:18 / D:22（确定性函数）。

**D-1 服务端 `/api/submit` 强化校验 + 限流**
- 原校验仅查 3 个字段存在性，无范围/类型/完整性，且无限流。攻击者可刷数据 / 写满磁盘。
- 新增 `validateRecord(rec)`：覆盖 `total`(0-100) / `levelId`(L1-L4) / `dimScores`(6 维每维 0-100) / `info.*`(类型+长度) / `answers`(qid 长度+四档分值+数量) / `packId` / `qVersion` / `ts`；
- 新增 `checkSubmitRate(ip)`：单 IP 滑动窗口，默认 10 次/60s（`APCA_SUBMIT_RATE_LIMIT` 环境变量可覆盖，便于单测与压测）；
- 限流优先于校验：超额直接 429，避免被利用做负载放大。

**D-2 工具脚本硬编码绝对路径清理**
- `tools/verify-web-dom.js` / `tools/verify-anchor-migration.js` 写死 `/Users/jmount/WorkBuddy/.../web` 与 `/Users/jmount/.workbuddy/binaries/.../linkedom`，**换机器即失败**。
- 改为 `__dirname` 推导仓库根 + linkedom 两级 fallback（标准 `require('linkedom')` → 受控环境 managed workspace），跨平台可移植。

**D-3 记录与分享带 `qVersion`（跨版本统计隔离）**
- 前端 `web/app.js`：测评记录构造处补 `qVersion: D.meta.questionSetVersion`、`framework: D.meta.framework`；分享 URL payload 同步带上；
- 后台 `server/server.js`：补字段长度/类型校验、缺省填充为 `'unknown'` 以兼容历史记录；
- 后台 `web/admin.js`：记录详情卡新增"题目版本"行，展示 `qVersion` 与 `framework`。

**D-4 byPack 按 (packId, qVersion) 分组（同名不同版本混池修复）**
- 原 `byPack` 仅按 `r.packName` 分组。若 v0.8.x 和 v0.9.x 都有"招投标专项"，同名的题包会被混在一个池里统计均值。
- 改为 `r.packId + '@' + r.qVersion`（'none' 归一为 '通用'），新版本 §1.14 的 byPack 回归断言锁定分组行为。

### 验证
- `node tools/verify-web.js`：**350 PASS / 0 FAIL**（新增 §1.14 + §1.15 共 9 条断言全绿）；
- `node tools/verify-web-dom.js`：**47 PASS / 0 FAIL**；
- `node tools/verify-wiring.js`：**19 PASS / 0 FAIL**；
- `node tools/verify-merge.js`：**11 PASS / 0 FAIL**；
- `node tools/verify-server.js`：**53 PASS / 0 FAIL**（v0.8.4 基线 40 → +13：7 条强化校验拒绝 + 3 条 byPack/qVersion + 2 条限流子进程测试 + 1 条文件计数）；
- **五件套合计 480 PASS / 0 FAIL**（v0.8.4 基线 458 → +22）；
- 题库内容、计分映射、解析措辞、乱序分布均与 v0.8.4 完全一致（A:19 / B:25 / C:18 / D:22，恒等排列 6 题）。

### 部署
- ⬜ 待部署（`rsync --delete` 同步 `web/` + `server/` → `scp` 更新日志 → `systemctl restart apca.service` → 线上验证 `v0.8.5` + 30 天地图 + 提交限流）。

---

## [v0.8.3] - 2026-09-04 · 更新日志 md→html 双载体 + 海报嵌入测评入口二维码

### 变更
- **日志文件改名：`CHANGELOG.md` → `系统更新日志.md`**；所有文档引用同步更新。
- **同步生成 `系统更新日志.html`**：`tools/build-update-html.js`（自包含 CSS + 极简 Markdown 渲染：H1–H4 / 列表 / 引用 / code / hr / 加粗 / 行内 code / em + 自动目录 + 锚点），每次发版后跑一次即可产出离线可读 HTML 版。
- **`tools/verify-web.js` 新增 §1.10 门禁**：HTML 必须存在、含当前版本号条目、文件 mtime 不早于 md（防产物过期/漏更）。
- **海报嵌入测评入口二维码**：
  - 新增 `tools/build-qrcode.py`（依赖 `qrcode[pil]`），预生成 `web/qrcode.png`（580×580 白底，指向 `http://<服务器IP>:8787/`，可用 `APCA_ASSESS_URL` 环境变量覆盖）。
  - `web/app.js` 的 `downloadPoster()` 改异步：先 `Image()` 加载 `qrcode.png?v=<version>`（用版本号做 query bypass 缓存），加载完成调 `drawPosterBody()`；二维码放海报左下角白底圆角块（150×150，留 10px 静默区），右侧三行配套引导文案。
  - QR 加载失败自动降级为占位块（不阻塞海报下载）。
- **海报同步迭代**：
  - `framework` 字段从遗留的 `'APCA v0.7'` 升级到 `'APCA v0.8.3'`（副标题显示修正）。
  - 下载文件名带版本号：`AI办公能力测评_<分>_<v0.8.3>.png`。
  - 页脚"扫码测你的 AI 办公能力"一行 + "生成于..." 收为右下三行（含当前版本号 + 卷别 + 网页版直入 + 生成时间）。
- **设计原则**：本次沿用项目一贯的零运行时依赖；HTML 渲染器纯 Node 内置实现，QR 码用 Python qrcode 库（已装到 managed venv）。

### 验证
- `node tools/verify-web.js`：含新增 §1.10 HTML 同步门禁，全量 PASS；
- 题库与计分未动，乱序分布仍 A:19 / B:25 / C:18 / D:22；`anchorBad=0`；
- 待跑 verify-web-dom / verify-wiring / verify-merge / verify-server 五件套后才算全绿。

### 部署
- ✅ 已上线（rsync + scp + `apca.service` 重启）：线上验证题库版本 `v0.8.3`、海报二维码与 `qrcode.png` 可访问、更新日志 HTML 版线上可读（中文文件名需 URL 百分号编码访问）。

---

## [v0.8.2] - 2026-09-04 · 测评人信息必填校验 + 发版文档同步门禁

### 变更
- **测评人信息页：姓名 / 所在公司 / 岗位改为必填项，但可虚拟填写**（不需要真实身份信息）。未填写时点击「开始答题」会被拦截并提示"请先填写姓名、公司、岗位（均可虚拟填写）后再开始测评。"；三个字段标签加红色 `*` 必填标记，输入框 placeholder 明示"必填，可虚拟填写"；测评批次与其他说明仍为选填。
- **消除"非必填"残留文案矛盾**：信息页顶部说明由"全部非必填"改为"带 * 的姓名、所在公司、岗位为必填项，均可虚拟填写"；关于页隐私条目由"信息页内容为自愿选填"改为"姓名、公司、岗位为必填但可虚拟填写，不必提供真实身份信息"，避免与新增校验自相矛盾。
- **发版门禁加固**：`tools/verify-web.js` 新增 **§1.8 版本号单点真值**（构建脚本与产物版本必须一致，防止只改 `data.js` 不改 `build-web-data.js` 的半成品发版）与 **§1.9 README 版本同步**（`README.md` 版本速览表须含当前版本号）；`CHANGELOG.md` 发版惯例补充"文档同步范围"与标准部署顺序。

### 验证
- 全量校验 **439 项 PASS / 0 FAIL**（计分 322 + DOM 47 + 接线 19 + 合并 11 + 服务端 40）；
- 新增 DOM 断言 3 条：必填项全空 / 仅填姓名 / 缺岗位三种情形点击「开始答题」均被拦截；同时补上 `window.alert` stub（linkedom 未实现，原会在拦截路径抛异常）；
- 题库与计分未改动，`v0.8.1` 的解析-锚点一致性、长度均衡、乱序分布（A:19 / B:25 / C:18 / D:22）全部保持。

### 部署
- ✅ 已上线（rsync + apca.service 重启）；线上验证题库版本 `v0.8.2`、信息页必填文案生效、"全部非必填 / 自愿选填"残留已清零。

---

## [v0.8.1] - 2026-09-04 · 计分锚点修正 + 历史数据迁移

### 变更
- **修正 12 道通用题"解析与分值矛盾"**（D2-1 / D3-1 / D3-3 / D3-5 / D4-1 / D4-2 / D4-4 / D4-5 / D5-1 / D5-4 / D5-5 / D6-5）：原解析贬低 D 选项（"不靠谱""放任""不可持续"）或推崇 A/B/C 为最优，与 D=100 的"最高成熟度锚点"计分矛盾。修法为**纯置换**——按解析公认成熟度排序重排选项位置（最优→D=100，最差→A=0），解析字母引用同步重排，文案一字未改，长度均衡不受影响。
- **历史数据自动迁移**：分值-内容映射变更后，浏览器端在启动时对 localStorage 历史记录 / 未完成进度 / 分享链接三类旧数据按上线时点（2026-09-04 01:00 GMT+8）自动换算分值并重算总分/维度/等级，幂等可重复执行；线上服务端 records.json 中 3 条真实提交记录已同步迁移（备份 records.json.bak-v071）。
- **新增工具**：`tools/check-analysis-consistency.js`（解析-分值一致性扫描）、`tools/verify-anchor-migration.js`（迁移功能测试）；`tools/verify-web.js` 新增 §1.6 锚点一致性长期门禁。

### 验证
- 全量校验 **432 项 PASS / 0 FAIL**（计分 318 + DOM 44 + 接线 19 + 合并 11 + 服务端 40）；
- 迁移表用改前备份逐内容交叉验证 **48/48 通过**；迁移功能测试 8/8 通过；
- 解析-分值一致性扫描硬矛盾 **0/84**；长度均衡 **0/84** 失衡（未破坏 v0.7.1 成果）。

### 部署
- ✅ 已上线（rsync + apca.service 重启），线上验证 v0.8.1 生效、D2-1 最优行为持 100 分且经乱序展示在 B 位。

---

## [v0.8] - 2026-09-03 · 每题选项乱序（防"总选 D 得高分"）

### 变更
- **架构**：源数据（miniprogram/data/）保持成熟度升序不变；`tools/build-web-data.js` 导出时按题目 ID 做确定性乱序（FNV-1a + mulberry32，`tools/lib/shuffle.js`）——选项文本**连同分值一起换位置**，每题输出独立 `scores` 字段，解析中的 A/B/C/D 字母引用同步重映射。
- **效果**：100 分选项在全库 84 题中的分布为 A:19 / B:25 / C:18 / D:22（D 仅 26%）。
- > ⚠️ **勘误（v0.8.4）**：本条目原写「无脑总选 D 预期得分降至约 35 分（L1）」，经实测复算**该数字错误**。正确实测值为**固定单字母期望分 A 47.2 / B 55.2 / C 48.4 / D 49.2**（随机作答期望≈50），固定选 B 可达 L3 低区。防作弊结论（乱序有效、无固定字母可套利）依然成立，但「35 分」不得再对外引用；v0.8.4 已加 §1.11 回归断言锁死该口径。
- **兼容性**：系统存储的是分值而非字母，历史记录、未完成进度、单独成卷合并出分全部天然兼容，无需迁移。
- **配套 UI 透明化**：复盘页每选项标注实际分值，"最高成熟度"标签跟随每题真正的 100 分选项；关于页与后台计分说明同步更新。

### 验证
- 全量校验 **431 项 PASS / 0 FAIL**（含 7 项新乱序校验：分值排列合法性 / 分布均匀性 / 选项内容与源数据等价 / 乱序确定性可复现 / 源数据未污染）。

### 部署
- ✅ 已上线，公网验证乱序分布与解析重映射正确（"D4到D8"等数字组合不误伤）。

---

## [v0.7.1] - 2026-09-03 · 题库答案长度均衡

### 变更
- **修正"最长=最优"视觉诱导缺陷**：84 题中 49 道严重失衡（最长选项字数达中位的 1.8–7.5 倍，且 48/49 道最长项恰是满分选项 D）。两轮补丁共改写 **65 题、260 处选项文案**——超长选项压缩冗余枚举、过短干扰项补充"看似具体但仍有缺陷"的细节；解析与 A/B/C/D 计分顺序不动。
- **新增工具**：`tools/check-option-balance.js`（四选项长度 ratio 扫描，阈值可调）。

### 验证
- 阈值 1.5 下失衡 **0/84**；全库最大 ratio 1.49、中位 1.23；全量校验 **423 项 PASS / 0 FAIL**。

### 部署
- ✅ 已上线。

---

## 网站版上线与应用层迭代 - 2026-09-03

- **网站版首版**：纯静态单页（`web/`），完全复用小程序 v0.7 题库与计分，零服务端（数据仅存本机 localStorage），支持进度续答、六维雷达、30 天提升地图、逐题复盘、结果分享（文字 / URL hash 链接 / 海报 PNG）、历史记录。
- **UX 修复**：结果卡显式展示 L1–L4 等级代号；复盘页 D 选项由"推荐"改为"最高成熟度锚点"标注并透明化计分规则。
- **测评人信息页**：答题前可选填姓名/公司/岗位/批次，随记录落库，支持按终端归并（clientId）。
- **管理后台**：零依赖 Node 服务（`server/server.js`）一体托管测评站与后台页——密码鉴权、令牌 8h、防爆破锁定、目录穿越防护、原子写库；后台含汇总概览 / 人员清单（搜索、筛选、CSV 导出）/ 题库 / 规则库四 Tab。纯静态分发形态保留（无服务端时静默降级）。
- **单独成卷合并出分**：专项单卷完成后与最近通用记录按 blended 同口径合并出分；无通用记录时单卷入口置灰。
- **产品改名**：「个人AI能力评测」→「个人AI办公能力测评」。
- **部署上线**：http://<服务器IP>:8787/（systemd 常驻）；后台密码已改强密码（systemd Environment，不进代码库）；登录框/图标细节优化；隐私文案按"云端部署"口径修正（服务端模式下如实标注结果同步至组织方服务器，本机模式自动回退"仅留本机"文案）。
- 验证：verify-server 40/40、verify-wiring 19/19、verify-merge 11/11；真实浏览器 E2E 全流程通过。

---

## [v0.7] - 2026-09-02 · 三套专项扩为完整独立模型

- 招投标 / 制造业 / 连锁零售三个专项各扩为 **18 题完整独立模型**（D1–D6 各 3 题、4 题型覆盖），各带独立维度权重（和为 1）；收敛 GEO / 数字市民包。
- 小程序侧隐私合规：`__usePrivacyCheck__` + 隐私授权弹层组件（相册保存场景）、首次启动隐私告知、关于页权限口径对齐；版本号全链路修正（app.js / project.config.json / 页脚）。
- 验证：小程序冒烟测试 108 项全绿。

## [v0.6] - 2026-09-02 · 通用测评 vs 专项模型双模式

- 定位纠正：APCA 是**通用测评**，专项为可选叠加的独立模型（自带标准+题库+入口+权重）。
- `buildQuestions(packId, mode)` 支持 blended（加测）/ standalone（单独成卷按专项权重计分）双模式；首页"仅测XX专项"独立入口。
- 验证：冒烟测试 107 项全绿。

## [v0.5] - 2026-09-02 · 海报分享 + 逐题复盘

- 结果海报（750×1080 canvas，等级+雷达+强弱项+小程序码，缺码自动降级）；逐题复盘（按维度分挑最弱已答题，附解析）；交卷存 answerTrail。

## [v0.4] - 2026-09-02 · 微信小程序工程

- 完整小程序工程：5 页面 + 6 组件 + data/utils/tools，零服务端设计；雷达图用旧版 Canvas API 兼容；断点续答 + 退出拦截。
- 踩坑沉淀：小程序无 computed 用 observers；bindtap 带参需无参包装；页面 wxss 覆盖 app.wxss 需双类选择器。

## [v0.3] - 2026-09-02 · 行业特化题包

- 4 个行业包（招投标/制造/GEO/数字市民，共 21 题）；首页卷别选择、题数联动、记录标注卷别。

## [v0.2] - 2026-09-02 · Demo 产品化

- localStorage 进度保存（中断可恢复）、成长记录（最近 20 次）、打印 PDF、返回首页确认保护。

## [v0.1] - 2026-09-02 · 初始版本

- APCA 指标体系：六大维度（AI 沟通/任务拆解/流程选择/质量管控/风险应对/经验复用）+ 4 级能力等级（L1 摸索 / L2 指令 / L3 流程 / L4 系统）；
- 30 道原创通用题，覆盖 4 种题型（情境决策/实际自评/方法反思/教别人）；
- 可交互单文件 HTML Demo；计分映射 A=0 / B=33.3 / C=66.7 / D=100（成熟度量表，非对错题）。
