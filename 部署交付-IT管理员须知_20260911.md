# APCA 内网部署交付单 · 交给 IT 管理员

> 生成日期：2026-09-11
> 内网服务已部署完成并通过自检，**当前只差公网映射这一段**。

---

## 一、内网侧（已完成，无需管理员操作）

| 项 | 值 |
|---|---|
| 内网机 IP | `<内网服务器IP>` |
| 服务端口 | **8787**（已 firewalld 永久放行） |
| 部署路径 | `<部署目录>/`（web / server / node 三个子目录） |
| 进程管理 | systemd 服务名 **`apca.service`**（已设开机自启） |
| 运行用户 | root |
| 自检结果 | `curl http://<内网服务器IP>:8787/api/ping` → `{"ok":true,...}` ✅ |

---

## 二、公网侧（需要管理员在 `<另一台服务器IP>` 上配置）

### 1. nginx 反向代理

在公网机的 nginx 配置目录（通常 `/etc/nginx/conf.d/`）新建 `site-b.conf`：

> **对外发布地址是子目录 `https://<内网域名>/ai-assess/`（注意末尾的 `/`），不是域名根路径。**
> 下面两处写法是硬性要求，写错任一处都会导致 404 或接口失效：
> - `proxy_pass` 结尾**必须带 `/`**：把 `/ai-assess/` 前缀剥掉再转发。否则 Node 收到的路径是 `/ai-assess/api/xxx`，而 `server.js` 只认 `/api/xxx`。
> - 必须把 `/ai-assess`（无尾斜杠）**301 跳到 `/ai-assess/`**：前端资源与接口全是相对路径（`data.js`、`api/submit`），页面地址不以 `/` 结尾时，浏览器会把 `api/submit` 解析成 `https://<内网域名>/api/submit`（错误）。

```nginx
server {
    listen 80;
    server_name <内网域名>;

    # 无尾斜杠 → 补尾斜杠（必需）
    location = /ai-assess {
        return 301 /ai-assess/;
    }

    location /ai-assess/ {
        proxy_pass http://<内网服务器IP>:8787/;   # 结尾斜杠 = 剥前缀（必需）

        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_read_timeout 60s;
        proxy_connect_timeout 10s;
    }
}
```

生效：

```bash
nginx -t && nginx -s reload
```

### 2. HTTPS（建议启用）

若 DNS 已解析到 `<另一台服务器IP>`：

```bash
certbot --nginx -d <内网域名>
```

若已有现成证书，请把证书路径告知，改用手动 `listen 443 ssl` 配置。

### 3. 防火墙 / 安全组

| 方向 | 端口 | 要求 |
|---|---|---|
| 公网 → 公网机 | 80 / 443 | 对全网放行 |
| 公网机 → 内网机 | 8787 | 放行（反代依赖） |
| 内网机 → 公网机 | 8787 | 可选，用于「双机数据互看」功能（见第三节） |

> 内网机 8787 **不需要**对公网直接开放——只经公网机反代访问。

---

## 三、可选：双机数据互看（如需要，请协助确认网络策略）

APCA 支持两台服务器互相读取测评记录（后台视图层聚合，不改变各自数据文件）。当前状态：

- **新内网机 → 老机（`<服务器IP>:8787`）**：实测**已通** ✅
- **老机 → 新内网机（`<内网服务器IP>:8787`）**：**未通**（老机在公网，访问内网需映射）

如要打通反向（老机后台也能看到内网机的数据），两个可选方案：

**方案一（推荐）**：在公网机 `<另一台服务器IP>` 上加一个内部转发路径：

```nginx
# 仅服务间调用，不对普通访客暴露
location /internal-apca/ {
    allow <服务器IP>;      # 只允许老机访问
    deny  all;
    proxy_pass http://<内网服务器IP>:8787/;
}
```

之后老机后台配置里把对端地址写成 `http://<另一台服务器IP>/internal-apca`。

**方案二**：在公网机上直接开一个仅对 `<服务器IP>` 放行的 8788 端口，转发到内网 8787。

> 如果暂时不需要双机互看，可跳过本节——内网机后台仍能看到老机的全部记录，功能不受影响。

---

## 四、验证清单（配置完成后自查）

```bash
# 1. 公网访问首页（注意结尾的斜杠）
curl -I https://<内网域名>/ai-assess/
#    期望：200

# 2. 公网访问服务接口
curl https://<内网域名>/ai-assess/api/ping
#    期望：{"ok":true,"service":"apca-admin-by-jmount","count":N}

# 3. 后台页可打开
curl -I https://<内网域名>/ai-assess/admin.html

# 4. 浏览器验证
#    https://<内网域名>/ai-assess/           → 测评首页
#    https://<内网域名>/ai-assess/admin.html → 管理后台（账号密码另行提供）
```

---

## 五、其他需要留意的一项（建议管理员关注）

内网机 `<内网服务器IP>` 的**系统时钟快 8 小时**、**时区配置为 `America/New_York`**：

```
chronyc tracking → System time: 28808 seconds fast of NTP time
timedatectl      → Time zone: America/New_York (EDT, -0400)
```

NTP 源本身正常，但偏差过大导致 chronyd 无法自动纠正。该问题会让所有带时间戳的业务数据（不只本服务）产生 8 小时偏移，建议一并修正：

```bash
timedatectl set-timezone Asia/Shanghai
chronyd -q 'server ntp.aliyun.com iburst'
systemctl restart chronyd
```

> 该修复影响全机业务，请管理员评估后决定执行时机。

---

## 六、联系方式与回执

配置完成后，请回执以下三项，便于我方做最终验收：

1. `curl https://<内网域名>/ai-assess/api/ping` 的输出
2. 是否已启用 HTTPS（以及证书到期时间）
3. 内网机时钟问题是否已修复
